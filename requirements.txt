# Core dependencies
cryptography
pycryptodome
loguru
minio==7.2.3
boto3==1.34.14  # AWS S3 support (optional - only needed for S3StorageAdapter)
python-dotenv

# Database: SQLAlchemy ORM with async support
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
alembic==1.13.1
greenlet==3.0.3

# Task Queue (Celery + Redis)
celery[redis]==5.3.6
redis==5.0.1

# FastAPI and async server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
email-validator==2.1.0

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# WebSocket support
websockets==12.0

# Audible library
audible

# Audible CLI (optional - for actual Audible functionality)
# audible-cli has dependency issues, install separately if needed:
# pip install audible-cli --no-deps
# Audible CLI dependencies (installed with --no-deps)
aiofiles
questionary
tabulate
toml
tqdm

# Testing
pytest==7.4.3
pytest-asyncio==0.23.3
httpx==0.27.2

# Linting and code quality
black==24.1.1
isort==5.13.2
flake8==7.0.0
flake8-docstrings==1.7.0
mypy==1.8.0
bandit==1.9.3
pytest-cov==4.1.0
